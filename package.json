{
  "name": "soulmkt-workspace",
  "private": true,
  "description": "仓库根仅作编排：前端与 Soul 数据唯一在 vessel/，勿在根目录再建 src/ 或 souls/",
  "scripts": {
    "dev": "npm run dev --prefix vessel",
    "build": "npm run build --prefix vessel",
    "lint": "npm run lint --prefix vessel",
    "preview": "npm run preview --prefix vessel",
    "verify": "node scripts/verify-soul-integrity.mjs",
    "verify:souls": "node scripts/verify-soul-integrity.mjs",
    "verify:strict": "node scripts/verify-soul-integrity.mjs --strict"
  }
}
