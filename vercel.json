{
  "installCommand": "npm install --prefix vessel",
  "buildCommand": "npm run build --prefix vessel",
  "outputDirectory": "vessel/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
