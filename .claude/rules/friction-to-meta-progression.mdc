---
description: Roguelike 元成长协议 —— 将AI遇到的局内摩擦（碰壁、出错、被用户否定等）转化为局外资产（经验），稳定打出轮椅 build
alwaysApply: false
---
# Friction → Meta-Progression 协议

> 每次 AI 出错不是终点，是一次局外升级的信号。

## 核心原则

Claude Code 是一款 roguelike 游戏。每个 session 是一局 run，上下文窗口清零即死亡。
**局内摩擦（misfire、纠错、重试）= 可压缩的信号，不是噪音。**
**局外成长系统**：就是在逐步调高自主滑块——不是一次性把 AI 放飞，而是每次摩擦后把一个具体的自主权写进系统，让 AI 在那个维度上不再需要你介入。

摩擦发生时，正确动作是：**提炼模式 → 写入局外系统 → 下一局带入**。

## 摩擦分类与沉淀目标

| 摩擦类型 | 沉淀位置 | 示例 |
|---|---|---|
| AI 反复在某决策点走错路 | `.cursor/rules/*.mdc` | 误删 `vessel/src/`、混淆目录结构 |
| 某类任务每次都要重新解释 | `.agents/skills/*/SKILL.md` | Soul 生成流程、竞品分析协议 |
| 项目状态 / 历史决策丢失 | `MEMORY.md` / `PROJECT_LOG.md` | 部署失败原因、架构决策 |
| AI 不知道「这个项目是谁」 | `SOUL.md` / harness prompt | 角色定位、禁止行为 |

## 触发条件（何时沉淀）

- 同一类错误出现 **≥2 次** → 必须写进 rule 或 skill
- 一次 session 中你**解释了 3 句以上**某个上下文 → 应该变成 MEMORY 或 rule
- 部署 / 构建失败被修复后 → 追加 `PROJECT_LOG.md`
- 某个任务流程跑通后 → 提炼成 SKILL.md 标准化

## 沉淀质量标准

✅ **可复现**：只沉淀能被再次触发的模式，不沉淀偶发异常  
✅ **单一关注点**：每条 rule / skill 只覆盖一个具体场景  
✅ **可验证**：写完后能在下一局 run 中验证它是否生效  
❌ 不要写「大而全」的 prompt，堆砌 ≠ 成长

## 轮椅 build 检验标准

当以下条件同时成立，视为该维度已达轮椅 build：

- 新 session 无需解释该上下文，AI 直接正确执行
- 该类摩擦在最近 5 次 session 中未再出现
- 规则 / skill 可被另一个 AI 实例复用，无需人工干预
